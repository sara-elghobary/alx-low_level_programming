#include <stdio.h>
#include "main.h"

/**
 * _islower - Checks if a character is lowercase
 *
 * @param c The character to check (@param)
 *
 * @return 1 if the character is lowercase, 0 otherwise (@return)
 *
 * @description This function checks if a character is lowercase.
 */
int _islower(int c)
{
return (islower(c));
}
